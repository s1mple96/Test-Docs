# 【任货行】客车账单轮扣MQ ACK异常告警消息优化测试用例

## 代码分析结果

**关键代码位置**：`NextDeductBillListener.java` 第56-57行

```java
DingTalkUtil.sendMsg("客车-nextDeductBillConsume 新账单轮扣方法Mq消费异常 msg:" + body
    + "\n error:" + RtxUtils.cutStrByLength(RtxUtils.getStackTrace(e), 300) + "， RabbitMQ-messageId:{}", messageId);
```

**改动点**：告警消息已包含 `messageId` 参数（第46行获取，第57行使用）

**测试方法**：在第53行 `channel.basicAck` 前添加异常抛出代码模拟ACK失败

---

## 1. 可直接验证用例（不需要修改代码）

### 1.1 账单正常轮扣流程验证

#### TC-001 账单正常轮扣功能验证
- **前置条件**：存在待轮扣的客车账单数据，MQ服务正常运行
- **测试步骤**：
  1. 触发账单轮扣任务
  2. 观察MQ消息消费日志
  3. 验证账单扣款结果
  4. 检查账单状态变更
- **预期结果**：账单正常完成轮扣，MQ消息正常ACK确认，无异常告警

#### TC-002 多条账单连续轮扣验证
- **前置条件**：存在多条待轮扣的客车账单数据
- **测试步骤**：
  1. 批量触发账单轮扣任务
  2. 观察各账单的轮扣处理过程
  3. 验证所有账单的扣款结果
- **预期结果**：所有账单正常完成轮扣，处理流程顺畅

#### TC-003 账单轮扣业务逻辑回归验证
- **前置条件**：新功能已部署
- **测试步骤**：
  1. 执行完整的账单轮扣流程
  2. 验证扣款金额、账单状态、流水记录
  3. 检查nextContinue字段处理逻辑
- **预期结果**：账单轮扣业务逻辑完全正常，数据准确

---

## 2. 需要修改代码验证用例（核心需求）

**代码修改方案**：在 `NextDeductBillListener.java` 第52行后添加测试代码

```java
// 消费确认前模拟异常（测试完成后删除）
if (body.contains("测试异常")) {
    throw new RuntimeException("模拟MQ ACK异常测试");
}
channel.basicAck(deliveryTag, false);
```

### 2.1 MQ ACK异常告警消息验证

#### TC-004 MQ ACK异常时企微告警包含Message Id
- **前置条件**：已修改代码添加测试异常，重新部署测试环境
- **测试步骤**：
  1. 构造包含"测试异常"关键字的账单轮扣消息
  2. 触发账单轮扣任务发送MQ消息
  3. 等待MQ消费端处理并触发异常
  4. 查看企微告警消息内容
- **预期结果**：企微告警消息格式为"客车-nextDeductBillConsume 新账单轮扣方法Mq消费异常 msg:xxx \n error:xxx， RabbitMQ-messageId:xxx"，messageId字段清晰可识别

#### TC-005 验证告警消息中Message Id的准确性
- **前置条件**：已触发MQ ACK异常告警
- **测试步骤**：
  1. 查看MQ发送日志，记录发送的Message Id
  2. 触发异常并获取企微告警消息
  3. 对比告警中的messageId与日志中的Message Id
- **预期结果**：告警消息中的messageId与MQ发送日志中的Message Id完全一致

#### TC-006 告警消息格式完整性验证
- **前置条件**：已触发MQ ACK异常告警
- **测试步骤**：
  1. 触发MQ消息监听异常
  2. 查看企微告警消息完整内容
  3. 验证消息包含：消息内容(msg)、错误堆栈(error，截取300字符)、messageId
- **预期结果**：告警消息格式完整，包含所有必要信息，messageId位置在消息末尾便于定位

#### TC-007 Message Id为空或null场景
- **前置条件**：模拟Message Id为空的异常场景（需开发配合）
- **测试步骤**：
  1. 构造Message Id为null的MQ消息
  2. 触发MQ ACK异常
  3. 查看企微告警消息
- **预期结果**：告警消息正常发送，messageId字段显示为null或空，不影响其他信息展示

#### TC-008 原有告警功能回归验证
- **前置条件**：新功能已部署
- **测试步骤**：
  1. 查看其他MQ监听类的异常告警（如BillInvalidGiveAmountRefundListener）
  2. 验证原有告警内容是否正常
  3. 确认告警发送机制未受影响
- **预期结果**：原有告警功能正常，其他监听类的告警不受影响

---

## 测试用例总结

| 分类 | 用例数 | 说明 |
|------|--------|------|
| 可直接验证 | 3 | 不需要修改代码 |
| 需要改代码验证 | 5 | 核心需求，需修改代码 |
| **合计** | **8** | - |

**执行顺序**：
1. **第一阶段**：执行TC-001、TC-002、TC-003（正常流程验证）
2. **第二阶段**：修改代码添加测试开关
3. **第三阶段**：执行TC-004、TC-005、TC-006、TC-007、TC-008（异常场景验证）

**测试重点**：
- P0：TC-004、TC-005、TC-006（核心需求验证，必须改代码）
- P1：TC-001、TC-002、TC-003（业务回归验证，可直接测）
- P2：TC-007、TC-008（边界和兼容性验证）

**代码修改位置**：
- 文件：`rtx-quartz/src/main/java/com/rtx/quartz/listener/NextDeductBillListener.java`
- 行号：第52行后
- 修改内容：添加异常抛出测试代码




