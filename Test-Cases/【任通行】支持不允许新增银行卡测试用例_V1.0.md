# 【任通行】支持不允许新增银行卡测试用例

**需求ID**: ID1017497  
**编写日期**: 2026-04-07  
**系统**: 任通行小程序 + 管理后台

---

## 1. 后台字典配置测试

### 1.1 渠道公司配置

#### TC-001 查看字典类型-不支持新增银行卡的渠道配置存在
- **前置条件**:
  - 系统: 任通行管理后台
  - 数据库: rtx（`sys_dict_type`、`sys_dict_data`）
- **测试步骤**:
  1. 登录任通行管理后台
  2. 进入「系统管理 → 字典管理」
  3. 查找字典名称"不支持新增银行卡的渠道配置"（dict_type: `enable_bind_bank_card_channel`）
- **预期结果**: 字典类型存在，备注说明为"子标签配置渠道id"

#### TC-002 新增渠道公司配置-正常新增
- **前置条件**:
  - 系统: 任通行管理后台
  - 字典类型 `enable_bind_bank_card_channel` 已存在
  - 准备一个有效的渠道ID（如 `0001`）
- **测试步骤**:
  1. 进入字典 `enable_bind_bank_card_channel` 的数据列表
  2. 点击新增，填写：标签=渠道名称（如"默认渠道-北京"），键值=`0001`
  3. 保存
  4. 验证 DB: `SELECT * FROM sys_dict_data WHERE dict_type='enable_bind_bank_card_channel' AND dict_value='0001' LIMIT 1;`
- **预期结果**: 新增成功，数据库中存在对应记录

#### TC-003 删除渠道公司配置-删除后恢复正常
- **前置条件**:
  - `enable_bind_bank_card_channel` 字典中存在某渠道配置
  - 测试用户名下所有有效车辆的 `channel_id` 均在该字典中
  - 此时用户小程序按钮应为隐藏状态
- **测试步骤**:
  1. 在管理后台删除该渠道字典数据
  2. 重新进入任通行小程序扣款设置页面
  3. 验证"新增代扣签约"按钮状态
  4. 进入账户信息页面，验证"新增签约银行卡"按钮状态
- **预期结果**: 删除配置后，两个页面的按钮均恢复显示

### 1.2 白名单配置

#### TC-004 查看字典类型-新增绑定银行卡白名单存在
- **前置条件**:
  - 系统: 任通行管理后台
  - 数据库: rtx（`sys_dict_type`）
- **测试步骤**:
  1. 登录管理后台，进入字典管理
  2. 查找字典名称"新增绑定银行卡白名单"（dict_type: `enable_bind_bank_card_whitelist`）
- **预期结果**: 字典类型存在，备注说明为"子标签和值都填入车牌号"

#### TC-005 新增白名单配置-车牌号格式
- **前置条件**:
  - 字典类型 `enable_bind_bank_card_whitelist` 已存在
  - 准备测试车牌号（如 `粤BZZJ05`）
- **测试步骤**:
  1. 进入白名单字典数据列表，点击新增
  2. 标签和键值均填写车牌号 `粤BZZJ05`（标签=键值=车牌号）
  3. 保存
  4. 验证 DB: `SELECT * FROM sys_dict_data WHERE dict_type='enable_bind_bank_card_whitelist' AND dict_value='粤BZZJ05' LIMIT 1;`
- **预期结果**: 新增成功，标签和键值均为车牌号

---

## 2. 小程序按钮隐藏场景测试

### 2.1 名下唯一有效车辆属于禁止渠道

#### TC-006 单辆车-正常状态-属于禁止渠道-隐藏按钮
- **前置条件**:
  - 系统: 任通行小程序
  - 字典配置: `enable_bind_bank_card_channel` 包含渠道 `0000`
  - 测试用户: 名下仅有1辆有效车辆，状态=正常(1)，`channel_id=0000`
  - 该车牌号不在白名单中
  - 数据准备 SQL:
    ```sql
    -- 查找符合条件的测试用户
    SELECT driver_user_id, car_num, channel_id, status 
    FROM rtx_etc_card_user 
    WHERE channel_id='0000' AND status='1' LIMIT 5;
    ```
- **测试步骤**:
  1. 使用符合条件的账号登录任通行小程序
  2. 进入「扣款设置」页面
  3. 观察"新增代扣签约"按钮是否存在
  4. 进入「账户信息」页面
  5. 观察"新增签约银行卡"按钮是否存在
- **预期结果**: 两个页面的新增银行卡按钮均不显示（隐藏）

#### TC-007 单辆车-黑名单状态-属于禁止渠道-隐藏按钮
- **前置条件**:
  - 字典配置: `enable_bind_bank_card_channel` 包含渠道 `0000`
  - 测试用户: 名下仅有1辆有效车辆，状态=黑名单(2)，`channel_id=0000`
  - 该车牌号不在白名单中
  - 参考测试账号: `driver_user_id=1341105333201`（车牌粤BZZJ05，channel_id=0000，status=2）
- **测试步骤**:
  1. 使用对应账号登录任通行小程序
  2. 进入「扣款设置」页面，观察"新增代扣签约"按钮
  3. 进入「账户信息」页面，观察"新增签约银行卡"按钮
- **预期结果**: 两个页面的新增银行卡按钮均隐藏（黑名单状态属于有效车辆，应计入判断）

#### TC-008 单辆车-待激活状态-属于禁止渠道-隐藏按钮
- **前置条件**:
  - 字典配置: `enable_bind_bank_card_channel` 包含渠道 `0000`
  - 测试用户: 名下仅有1辆有效车辆，状态=待激活(3)，`channel_id=0000`
  - 该车牌号不在白名单中
  - 数据准备: 找或创建 `channel_id=0000, status=3` 的车辆记录
- **测试步骤**:
  1. 使用对应账号登录任通行小程序
  2. 验证两个页面的按钮均隐藏
- **预期结果**: 两个页面新增银行卡按钮均隐藏（待激活状态属于有效车辆）

### 2.2 名下多辆有效车辆全属于禁止渠道

#### TC-009 多辆车-全部有效状态-全属于禁止渠道-隐藏按钮
- **前置条件**:
  - 字典配置: `enable_bind_bank_card_channel` 包含渠道 `0000`
  - 测试用户: 名下有2辆及以上有效车辆，`channel_id` 均为 `0000`，状态均为有效（1/2/3）
  - 所有车牌均不在白名单中
- **测试步骤**:
  1. 使用对应账号登录任通行小程序
  2. 验证「扣款设置」页面和「账户信息」页面的按钮均隐藏
- **预期结果**: 两个页面新增银行卡按钮均隐藏

### 2.3 名下有非有效状态车辆但有效车辆全在禁止渠道

#### TC-010 有已注销车辆(非禁止渠道)+有效车辆全属禁止渠道-隐藏按钮
- **前置条件**:
  - 字典配置: `enable_bind_bank_card_channel` 包含渠道 `0000`
  - 测试用户: 
    - 车辆A: 状态=已注销(4)，channel_id=非0000渠道
    - 车辆B: 状态=正常(1)，channel_id=0000
  - 所有车牌均不在白名单中
- **测试步骤**:
  1. 使用对应账号登录任通行小程序
  2. 验证两个页面按钮均隐藏
- **预期结果**: 按钮隐藏（注销状态车辆被跳过，不影响禁止判断；有效车辆全在禁止渠道）

#### TC-011 有未发行车辆(非禁止渠道)+有效车辆全属禁止渠道-隐藏按钮
- **前置条件**:
  - 字典配置: `enable_bind_bank_card_channel` 包含渠道 `0000`
  - 测试用户:
    - 车辆A: 状态=未发行(0)，channel_id=非0000渠道
    - 车辆B: 状态=正常(1)，channel_id=0000
  - 所有车牌均不在白名单中
- **测试步骤**:
  1. 使用对应账号登录任通行小程序
  2. 验证两个页面按钮均隐藏
- **预期结果**: 按钮隐藏（未发行状态车辆被跳过，不影响禁止判断）

---

## 3. 小程序按钮显示场景测试

### 3.1 字典未配置禁止渠道

#### TC-012 字典渠道配置为空-显示按钮
- **前置条件**:
  - 字典 `enable_bind_bank_card_channel` 无任何数据（或清空后测试）
  - 任意测试用户
- **测试步骤**:
  1. 确认字典中无渠道配置: `SELECT * FROM sys_dict_data WHERE dict_type='enable_bind_bank_card_channel';`
  2. 登录任通行小程序
  3. 验证两个页面按钮均显示
- **预期结果**: 两个页面新增银行卡按钮均正常显示

### 3.2 名下有有效车辆不属于禁止渠道

#### TC-013 多辆车-部分属于禁止渠道-部分不属于-显示按钮
- **前置条件**:
  - 字典配置: `enable_bind_bank_card_channel` 包含渠道 `0000`
  - 测试用户:
    - 车辆A: 状态=待激活(3)，channel_id=0000（在禁止渠道）
    - 车辆B: 状态=待激活(3)，channel_id=`f575f279...`（不在禁止渠道）
  - 所有车牌均不在白名单中
  - 参考测试账号: `driver_user_id=1341105333202`（粤BAT0001 channel_id=0000+粤BZZJ14 channel_id=f575...，均status=3）
- **测试步骤**:
  1. 使用对应账号登录任通行小程序
  2. 进入「扣款设置」页面，观察"新增代扣签约"按钮
  3. 进入「账户信息」页面，观察"新增签约银行卡"按钮
- **预期结果**: 两个页面新增银行卡按钮均显示（存在有效车辆不在禁止渠道）

#### TC-014 名下所有有效车辆均不属于禁止渠道-显示按钮
- **前置条件**:
  - 字典配置: `enable_bind_bank_card_channel` 包含渠道 `0000`
  - 测试用户: 名下所有有效车辆的 `channel_id` 均不为 `0000`
  - 所有车牌均不在白名单中
- **测试步骤**:
  1. 使用对应账号登录任通行小程序
  2. 验证两个页面按钮均显示
- **预期结果**: 两个页面新增银行卡按钮均正常显示

### 3.3 用户名下无有效车辆

#### TC-015 用户名下无任何车辆-显示按钮
- **前置条件**:
  - 字典配置: `enable_bind_bank_card_channel` 包含渠道 `0000`
  - 测试用户: `rtx_etc_card_user` 中无该用户的任何车辆记录
- **测试步骤**:
  1. 使用无车辆记录的账号登录任通行小程序
  2. 验证两个页面按钮均显示
- **预期结果**: 按钮显示（无车辆无法触发禁止条件）

#### TC-016 用户名下车辆全为已注销状态-显示按钮
- **前置条件**:
  - 字典配置: `enable_bind_bank_card_channel` 包含渠道 `0000`
  - 测试用户: 名下车辆状态全部为已注销(4)或未发行(0)，`channel_id=0000`
  - 所有车牌均不在白名单中
  - 参考: `driver_user_id=13411053332`（所有channel_id=0000的车辆均为 status=4 或 status=0）
- **测试步骤**:
  1. 使用对应账号登录任通行小程序
  2. 验证两个页面按钮均显示
- **预期结果**: 按钮显示（所有车辆均为非有效状态，被跳过，未触发禁止逻辑）

---

## 4. 白名单豁免测试

#### TC-017 名下车辆全在禁止渠道但有车牌在白名单-显示按钮
- **前置条件**:
  - 字典配置: `enable_bind_bank_card_channel` 包含渠道 `0000`
  - 字典配置: `enable_bind_bank_card_whitelist` 包含某车牌（如 `粤BZZJ05`）
  - 测试用户:
    - 车辆A: 车牌=`粤BZZJ05`，状态=黑名单(2)，channel_id=0000
    - 车辆B: 状态=正常(1)，channel_id=0000
  - 参考: `driver_user_id=1341105333201`，其中粤BZZJ05 status=2, channel_id=0000
- **测试步骤**:
  1. 在管理后台将 `粤BZZJ05` 加入白名单字典
  2. 使用 `driver_user_id=1341105333201` 对应账号登录小程序
  3. 验证两个页面按钮均显示
  4. 测试完成后，从白名单中移除 `粤BZZJ05`（恢复环境）
- **预期结果**: 白名单中的车牌触发豁免，按钮显示（即使其余车辆全在禁止渠道）

#### TC-018 白名单车牌为已注销状态-仍触发豁免-显示按钮
- **前置条件**:
  - 字典配置: 渠道禁止列表包含 `0000`，白名单包含某已注销车辆的车牌
  - 测试用户: 
    - 车辆A: 车牌在白名单，状态=已注销(4)
    - 车辆B: 状态=正常(1)，channel_id=0000
- **测试步骤**:
  1. 确认白名单字典包含已注销车辆的车牌
  2. 使用对应账号登录小程序
  3. 验证两个页面按钮状态
- **预期结果**: 按钮显示（白名单检查优先于状态过滤，已注销车牌仍可触发豁免）

#### TC-019 用户所有车辆车牌均在白名单-显示按钮
- **前置条件**:
  - 字典配置: 渠道禁止列表包含 `0000`
  - 字典配置: 白名单包含该用户所有车辆的车牌号
  - 测试用户: 名下所有车辆 channel_id=0000
- **测试步骤**:
  1. 使用对应账号登录小程序
  2. 验证两个页面按钮均显示
- **预期结果**: 按钮显示

#### TC-020 白名单精确匹配-车牌大小写完全一致
- **前置条件**:
  - 白名单配置了某车牌（如 `粤BZZJ05`）
  - 数据库中该用户车辆的 `car_num` 值与白名单配置的 `dict_value` 完全一致
- **测试步骤**:
  1. 确认数据库中 `dict_value` 与 `rtx_etc_card_user.car_num` 的值完全匹配（大小写、空格）
  2. 使用对应账号登录小程序
  3. 验证按钮状态符合预期（匹配 → 显示，不匹配 → 隐藏）
- **预期结果**: 白名单严格匹配，车牌号完全一致时才触发豁免

---

## 5. 按钮位置双验证测试

#### TC-021 扣款设置页面-新增代扣签约按钮-条件满足时隐藏
- **前置条件**:
  - 测试用户: 名下所有有效车辆均属于禁止渠道，且无白名单
  - 字典配置正确
- **测试步骤**:
  1. 使用对应账号登录任通行小程序
  2. 进入「扣款设置」页面
  3. 确认"新增代扣签约"按钮不可见
- **预期结果**: "新增代扣签约"按钮不显示

#### TC-022 账户信息页面-新增签约银行卡按钮-条件满足时隐藏
- **前置条件**:
  - 同 TC-021
- **测试步骤**:
  1. 使用对应账号登录任通行小程序
  2. 进入「账户信息」页面
  3. 确认"新增签约银行卡"按钮不可见
- **预期结果**: "新增签约银行卡"按钮不显示

#### TC-023 扣款设置页面-条件不满足时正常显示按钮
- **前置条件**:
  - 测试用户: 名下有至少1辆有效车辆不属于禁止渠道（或字典无配置）
- **测试步骤**:
  1. 使用对应账号登录小程序
  2. 进入「扣款设置」页面
  3. 确认"新增代扣签约"按钮正常显示
  4. 进入「账户信息」页面
  5. 确认"新增签约银行卡"按钮正常显示
- **预期结果**: 两个页面的按钮均正常显示，点击后功能正常

---

## 6. 回归测试

#### TC-024 非禁止渠道用户-新增银行卡全流程正常
- **前置条件**:
  - 测试用户: 名下车辆 `channel_id` 不在禁止渠道字典中
  - 系统功能正常
- **测试步骤**:
  1. 登录任通行小程序
  2. 进入「扣款设置」，点击"新增代扣签约"按钮
  3. 完成银行卡新增流程
  4. 验证银行卡新增成功
- **预期结果**: 新增银行卡全流程正常，功能不受影响

#### TC-025 已在禁止渠道用户-切换到非禁止渠道后-按钮恢复显示
- **前置条件**:
  - 测试用户: 名下车辆原 `channel_id=0000`（禁止渠道），按钮已隐藏
  - 将该用户的车辆 `channel_id` 改为非禁止渠道的值（需开发配合或直接改DB）
- **测试步骤**:
  1. 修改用户车辆的 `channel_id` 为非禁止渠道
  2. 重新登录小程序（或刷新）
  3. 验证两个页面按钮恢复显示
- **预期结果**: 按钮恢复显示，逻辑动态响应字典和数据变化
