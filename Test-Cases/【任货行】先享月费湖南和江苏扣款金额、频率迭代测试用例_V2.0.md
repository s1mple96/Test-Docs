# 【任货行】先享月费湖南和江苏扣款金额、频率迭代测试用例

> 版本：V2.0（2026-03-13 根据 commit 630c71caf 代码更新重新生成）

---

## 1. 账单金额验证（月管理费30元）

### 1.1 eTCBuckleFee 任务生成失败账单金额验证

#### TC-001 XTK湖南运营商-月管理费失败账单金额为30元
- **前置条件**:
  - 系统: 任货行定时任务 (hcbTimer-business)
  - 数据库: hcb
  - 用户: 湖南XTK运营商用户（有 Silent_Wallet 标签，无扣款通道配置）
  - 验证当前风控任务配置不含 XTK 的 JSON 扣款金额，或 XTK 金额在 `jsonObject` 中无对应 key
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 查询 XTK 运营商已有月管理费失败账单：
     ```sql
     SELECT id, bill_amount, fail_amount, operator_code, bill_status, bill_date
     FROM hcb_complete_wallet_bill
     WHERE operator_code = 'XTK' AND bill_type = 1 AND bill_status = 2
     ORDER BY create_time DESC LIMIT 5;
     ```
  2. SSH 执行 `eTCBuckleFee` 任务触发：
     ```bash
     curl "http://192.168.1.60:8087/hcbTimer-bill/test/testETCBuckleFee.do"
     ```
  3. 查询任务执行后新生成的失败账单：
     ```sql
     SELECT id, bill_amount, fail_amount, operator_code, bill_status, bill_date
     FROM hcb_complete_wallet_bill
     WHERE operator_code = 'XTK' AND bill_type = 1 AND bill_status = 2
     ORDER BY create_time DESC LIMIT 5;
     ```
  4. 检查日志确认金额：
     ```bash
     tail -n 200 /home/tomcat/tomcat-hcbTimer-bill/logs/catalina.out | grep -i "XTK\|30\|月管理费"
     ```
- **预期结果**: 新生成的 XTK 失败账单 `bill_amount = 30.00`，`fail_amount = 30.00`，备注为"配置中没有该运营商的配置，不进行扣款"

---

#### TC-002 TXB江苏运营商-月管理费失败账单金额为30元
- **前置条件**:
  - 系统: 任货行定时任务 (hcbTimer-business)
  - 数据库: hcb
  - 用户: 江苏TXB运营商用户（有 Silent_Wallet 标签，无扣款通道配置）
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 查询TXB运营商现有月管理费失败账单基线：
     ```sql
     SELECT id, bill_amount, fail_amount, operator_code, bill_status
     FROM hcb_complete_wallet_bill
     WHERE operator_code = 'TXB' AND bill_type = 1 AND bill_status = 2
     ORDER BY create_time DESC LIMIT 5;
     ```
  2. 触发 `eTCBuckleFee` 任务，等待执行完毕
  3. 查询新生成的 TXB 失败账单
  4. 验证账单金额字段
- **预期结果**: 新生成的 TXB 失败账单 `bill_amount = 30.00`，`fail_amount = 30.00`，与旧逻辑（15元）相比已翻倍

---

#### TC-003 LTK黑龙江运营商-月管理费金额不受影响
- **前置条件**:
  - 系统: 任货行定时任务 (hcbTimer-business)
  - 数据库: hcb
  - 用户: 黑龙江 LTK 运营商用户
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 查询 LTK 运营商在风控任务配置中的扣款金额（`Fee_Silent_Wallet` 任务的 `task_require` JSON）：
     ```sql
     SELECT task_code, task_require
     FROM hcb_label_security_task
     WHERE task_code = 'Fee_Silent_Wallet';
     ```
  2. 记录 LTK 对应金额（应为配置值，非30元）
  3. 触发 `eTCBuckleFee` 任务
  4. 查询 LTK 新生成账单：
     ```sql
     SELECT bill_amount, fail_amount, operator_code
     FROM hcb_complete_wallet_bill
     WHERE operator_code = 'LTK' AND bill_type = 1
     ORDER BY create_time DESC LIMIT 3;
     ```
- **预期结果**: LTK 账单金额与配置一致，未受本次改动影响；只有在配置中不存在 LTK key 时才会生成30元失败账单

---

#### TC-004 MTK内蒙古运营商-月管理费金额不受影响
- **前置条件**:
  - 系统: 任货行定时任务 (hcbTimer-business)
  - 数据库: hcb
  - 用户: 内蒙古 MTK 运营商用户
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 查询MTK在 `Fee_Silent_Wallet` 配置中的扣款金额
  2. 触发任务，验证 MTK 用户生成的账单金额与配置一致
  3. 对比 XTK/TXB 新增的30元失败账单，确认 MTK 逻辑独立
- **预期结果**: MTK 账单金额按原配置值执行，不变为30元

---

## 2. 账单生成频率验证（偶数月2号执行）

### 2.1 Quartz Cron 配置验证

#### TC-005 偶数月2号-账单生成任务自动触发
- **前置条件**:
  - 系统: 任货行定时任务 (hcbTimer-business)
  - 配置文件: `spring-scheme-business.xml`（`monthlyPaylaterGenerateTaskJobTrigger` bean）
  - 新 Cron：`0 0 4 2 2,4,6,8,10,12 ? *`
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 查看 Spring Quartz 配置，确认 Cron 表达式：
     ```bash
     grep -A 5 "monthlyPaylaterGenerateTaskJobTrigger" /home/java-server/hcbTimer-business/WEB-INF/classes/spring/spring-scheme-business.xml
     ```
  2. 确认 Cron：`0 0 4 2 2,4,6,8,10,12 ? *`（每年2,4,6,8,10,12月2号凌晨4点执行）
  3. 手动触发模拟（偶数月2号）：
     ```bash
     curl "http://127.0.0.1:8082/hcbTimer-business/test/startMonthlyTask.do?type=1&date=20260402"
     ```
  4. 查询是否生成 `hcb_monthly_paylater_wallet_bill` 账单
- **预期结果**: 任务成功执行，生成先享月费账单；Cron 配置包含 2,4,6,8,10,12 月份

---

#### TC-006 奇数月-账单生成任务不触发（Cron配置验证）
- **前置条件**:
  - 系统: 任货行定时任务 (hcbTimer-business)
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 查看 Cron 配置，确认不包含奇数月：
     ```bash
     grep "monthlyPaylaterGenerateTaskJobTrigger" -A 10 /home/.../spring-scheme-business.xml
     ```
  2. 确认 Cron 月份字段为 `2,4,6,8,10,12`（不含1,3,5,7,9,11）
- **预期结果**: 奇数月不自动执行账单生成任务，Cron 配置正确

---

## 3. 账单生成时间窗口验证（改为1个月）

### 3.1 账单日期范围验证

#### TC-007 账单生成只汇总最近1个月内的失败月管理费账单
- **前置条件**:
  - 系统: 任货行定时任务 (hcbTimer-business)
  - 数据库: hcb
  - 造数: 准备2组失败账单：
    - 组A（近1个月内）：`bill_date` 在 `CURDATE()-30天` 到 `CURDATE()` 之间，operator_code=XTK，bill_type=1，bill_status=2，bill_amount=30
    - 组B（超过1个月）：`bill_date` 在 `CURDATE()-60天` 之前，同样条件
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 造数（见前置条件），清空旧的先享月费账单：
     ```sql
     DELETE FROM hcb_monthly_paylater_wallet_bill WHERE id_code = '你的身份证';
     ```
  2. 插入组A和组B的月管理费失败账单（`hcb_complete_wallet_bill`）
  3. 手动触发账单生成（date=今天，YYYYMMDD格式）：
     ```bash
     curl "http://127.0.0.1:8082/hcbTimer-business/test/startMonthlyTask.do?type=1&date=20260313"
     ```
  4. 查询生成的先享月费账单：
     ```sql
     SELECT id, wallet_id, bill_amount, bill_date, operator_code, bill_status
     FROM hcb_monthly_paylater_wallet_bill
     WHERE id_code = '你的身份证'
     ORDER BY create_time DESC;
     ```
- **预期结果**:
  - 只汇总组A（1个月内）的失败账单，生成先享月费账单
  - 组B（超过1个月）的账单不被汇总
  - 先享月费账单金额 = 30元（组A汇总金额）

---

#### TC-008 账单生成传入指定日期-正确计算时间窗口
- **前置条件**:
  - 系统: 任货行定时任务 (hcbTimer-business)
  - 数据库: hcb
  - 造数: 在 2026-02-01 到 2026-03-02 范围内的XTK失败月管理费账单（bill_amount=30）
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 插入2月份失败账单（`bill_date` = 20260210，20260215，20260220，operator_code=XTK）
  2. 手动触发，指定 date=20260302（即3月2号的时间窗口，查2月1日到3月2日）：
     ```bash
     curl "http://127.0.0.1:8082/hcbTimer-business/test/startMonthlyTask.do?type=1&date=20260302"
     ```
  3. 查询生成的先享月费账单，核对是否包含2月份账单
  4. 查看日志：
     ```bash
     tail -n 200 /home/tomcat/tomcat-hcbTimer-bill/logs/catalina.out | grep "先享月费钱包账单生成"
     ```
- **预期结果**:
  - 生成先享月费账单，涵盖2月份的失败账单
  - 账单金额为30元
  - 日志显示 `userList size > 0`，任务正常完成

---

#### TC-009 超过1个月的旧账单不被汇总（边界值）
- **前置条件**:
  - 数据库: hcb
  - 造数: `bill_date = (当前date的上上月1号 - 1天)`，即刚好超出1个月窗口
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 造边界数据，bill_date 等于 `billDate.minusMonths(1).withDayOfMonth(1)` 减1天
  2. 触发账单生成，date=今天
  3. 查询是否被汇总到先享月费账单中
- **预期结果**: 该笔账单不被汇总，不生成对应先享月费账单

---

## 4. 账单扣款频率验证（奇数月1号执行）

### 4.1 Quartz Cron 扣款配置验证

#### TC-010 奇数月1号-账单扣款任务自动触发
- **前置条件**:
  - 系统: 任货行定时任务 (hcbTimer-business)
  - 配置: `monthlyPaylaterDeductTaskJobTrigger` bean
  - 新 Cron：`0 0 4 1 1,3,5,7,9,11 ? *`（每年1,3,5,7,9,11月1号凌晨4点执行）
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 查看 Cron 配置确认：
     ```bash
     grep -A 5 "monthlyPaylaterDeductTaskJobTrigger" /home/java-server/hcbTimer-business/WEB-INF/classes/spring/spring-scheme-business.xml
     ```
  2. 确认扣款任务 Cron 包含 1,3,5,7,9,11 月份
  3. 手动触发扣款（type=2）：
     ```bash
     curl "http://127.0.0.1:8082/hcbTimer-business/test/startMonthlyTask.do?type=2&date=20260301"
     ```
  4. 确认日志中扣款任务正常启动
- **预期结果**: Cron 配置正确，手动触发扣款成功，日志输出"先享月费账单扣款开始"

---

#### TC-011 偶数月-账单扣款任务不触发（Cron配置验证）
- **前置条件**:
  - 配置文件: `spring-scheme-business.xml`
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 查看扣款任务 Cron，确认月份字段为 `1,3,5,7,9,11`
  2. 确认不含偶数月（2,4,6,8,10,12）
- **预期结果**: 偶数月不执行自动扣款，Cron 配置正确

---

## 5. 手动补跑3月数据验证

### 5.1 补跑历史账单生成

#### TC-012 手动触发3月先享月费账单生成（指定日期）
- **前置条件**:
  - 系统: 任货行定时任务 (hcbTimer-business)
  - 数据库: hcb
  - 3月份已有 XTK/TXB 的月管理费失败账单（bill_date在20260301附近，bill_amount=30）
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 确认3月份 XTK/TXB 失败月管理费账单存在：
     ```sql
     SELECT COUNT(*), operator_code, SUM(fail_amount)
     FROM hcb_complete_wallet_bill
     WHERE bill_type = 1 AND bill_status = 2
       AND bill_date BETWEEN 20260201 AND 20260313
       AND operator_code IN ('XTK', 'TXB')
     GROUP BY operator_code;
     ```
  2. 清除已有先享月费账单（测试数据）：
     ```sql
     DELETE FROM hcb_monthly_paylater_wallet_bill
     WHERE operator_code IN ('XTK', 'TXB') AND id_code = '你的身份证';
     ```
  3. 手动触发账单生成（指定3月2号）：
     ```bash
     curl "http://127.0.0.1:8082/hcbTimer-business/test/startMonthlyTask.do?type=1&date=20260302"
     ```
  4. 等待任务完成，查询生成的先享月费账单：
     ```sql
     SELECT wallet_id, bill_amount, operator_code, bill_status, bill_date
     FROM hcb_monthly_paylater_wallet_bill
     WHERE operator_code IN ('XTK', 'TXB') AND id_code = '你的身份证'
     ORDER BY create_time DESC LIMIT 10;
     ```
  5. 查看日志：
     ```bash
     tail -n 300 /home/tomcat/tomcat-hcbTimer-bill/logs/catalina.out | grep "先享月费"
     ```
- **预期结果**:
  - 生成 XTK 和 TXB 的先享月费账单
  - 账单金额 = 30元
  - 账单状态 `bill_status = 0`（待扣款）
  - 日志无 ERROR

---

#### TC-013 手动触发3月先享月费扣款（按身份证分批）
- **前置条件**:
  - 数据库: hcb
  - 已有待扣款先享月费账单（`bill_status=0`，operator_code=XTK/TXB，bill_amount=30）
  - 用户钱包冻结余额充足（`FREEZE_AMOUNT - WITHHOLD_FREEZE_AMOUNT >= 30`）
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 确认待扣款账单和钱包余额：
     ```sql
     SELECT mw.wallet_id, mw.bill_amount, mw.bill_status,
            w.FREEZE_AMOUNT, w.WITHHOLD_FREEZE_AMOUNT
     FROM hcb_monthly_paylater_wallet_bill mw
     JOIN hcb_hlj_truckuserwallet w ON mw.wallet_id = w.WALLET_ID
     WHERE mw.id_code = '你的身份证' AND mw.bill_status = 0;
     ```
  2. 按身份证触发扣款（小批量）：
     ```bash
     curl "http://127.0.0.1:8082/hcbTimer-business/test/startMonthlyTaskByIdCode.do?idCodeStrs=你的身份证"
     ```
  3. 查看日志确认扣款结果：
     ```bash
     tail -n 300 /home/tomcat/tomcat-hcbTimer-bill/logs/catalina.out | grep "先享月费账单扣款"
     ```
  4. 查询账单状态更新：
     ```sql
     SELECT wallet_id, bill_amount, actual_amount, bill_status, remark
     FROM hcb_monthly_paylater_wallet_bill
     WHERE id_code = '你的身份证' ORDER BY update_time DESC;
     ```
- **预期结果**:
  - 账单状态从 `bill_status=0` 更新为 `bill_status=1`（扣款成功）
  - `actual_amount = 30.00`
  - 日志中有"按钱包扣款_walletId"和扣款成功记录
  - `hcb_hlj_etc_flow` 中新增 `TRADE_TYPE=29` 的先享月费扣款流水

---

## 6. 历史数据初始化验证

### 6.1 1-3月份失败账单金额更新为30元

#### TC-014 初始化1月份XTK/TXB失败月管理费账单金额为30元
- **前置条件**:
  - 数据库: hcb
  - 2026年1月份已有 XTK/TXB 月管理费失败账单（`bill_amount = 15`，`bill_status = 2`，`bill_type = 1`）
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 查询1月份现有账单金额（基线）：
     ```sql
     SELECT COUNT(*), operator_code, SUM(bill_amount), SUM(fail_amount)
     FROM hcb_complete_wallet_bill
     WHERE bill_type = 1 AND bill_status = 2
       AND bill_date BETWEEN 20260101 AND 20260131
       AND operator_code IN ('XTK', 'TXB')
     GROUP BY operator_code;
     ```
  2. 执行SQL初始化（先 COUNT 确认数量）：
     ```sql
     SELECT COUNT(*) FROM hcb_complete_wallet_bill
     WHERE bill_type = 1 AND bill_status = 2
       AND bill_date BETWEEN 20260101 AND 20260131
       AND operator_code IN ('XTK', 'TXB')
       AND bill_amount = 15;
     ```
  3. 执行更新：
     ```sql
     UPDATE hcb_complete_wallet_bill
     SET bill_amount = 30, fail_amount = 30, update_time = NOW()
     WHERE bill_type = 1 AND bill_status = 2
       AND bill_date BETWEEN 20260101 AND 20260131
       AND operator_code IN ('XTK', 'TXB')
       AND bill_amount = 15;
     ```
  4. 验证更新结果：
     ```sql
     SELECT COUNT(*), operator_code, SUM(bill_amount)
     FROM hcb_complete_wallet_bill
     WHERE bill_type = 1 AND bill_status = 2
       AND bill_date BETWEEN 20260101 AND 20260131
       AND operator_code IN ('XTK', 'TXB')
     GROUP BY operator_code;
     ```
- **预期结果**: 1月份所有 XTK/TXB 失败账单 `bill_amount = 30.00`，`fail_amount = 30.00`；更新数量与步骤2查到的COUNT一致；无15元账单残留

---

#### TC-015 初始化2月份XTK/TXB失败月管理费账单金额为30元
- **前置条件**:
  - 数据库: hcb
  - 2026年2月份已有 XTK/TXB 月管理费失败账单（`bill_amount = 15`）
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 同 TC-014 步骤，日期范围改为 `20260201 AND 20260228`
  2. 确认更新数量，执行 UPDATE
  3. 验证更新后所有2月份账单金额为30元
- **预期结果**: 2月份所有 XTK/TXB 失败账单金额更新为30元

---

#### TC-016 初始化3月份XTK/TXB失败月管理费账单金额为30元
- **前置条件**:
  - 数据库: hcb
  - 2026年3月份已有 XTK/TXB 月管理费失败账单（`bill_amount = 15`，`bill_date BETWEEN 20260301 AND 20260313`）
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 查询3月份现有账单（含3月1号）：
     ```sql
     SELECT COUNT(*), operator_code, bill_amount
     FROM hcb_complete_wallet_bill
     WHERE bill_type = 1 AND bill_status = 2
       AND bill_date BETWEEN 20260301 AND 20260313
       AND operator_code IN ('XTK', 'TXB')
     GROUP BY operator_code, bill_amount;
     ```
  2. 确认15元账单数量，执行更新
  3. 验证3月份所有账单金额为30元
- **预期结果**: 3月份所有 XTK/TXB 失败账单金额更新为30元；为后续手动触发生成先享月费账单（30元）做好准备

---

#### TC-017 历史数据初始化不影响LTK/MTK运营商账单
- **前置条件**:
  - 数据库: hcb
  - LTK/MTK 运营商有现存月管理费失败账单
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 查询 LTK/MTK 账单金额基线
  2. 执行 XTK/TXB 的初始化 SQL（WHERE 中含 `operator_code IN ('XTK', 'TXB')`）
  3. 再次查询 LTK/MTK 账单金额
- **预期结果**: LTK/MTK 账单金额不变，初始化操作仅影响 XTK/TXB

---

## 7. 全量钱包应付月管理费更新验证

### 7.1 paid_monthly_amount 字段更新

#### TC-018 全量钱包paid_monthly_amount增加15元（每笔30元账单+15）
- **前置条件**:
  - 数据库: hcb
  - 已有 XTK/TXB 用户的 `hcb_complete_wallet` 记录，`paid_monthly_amount` 有历史值
  - 了解更新范围：1-3月份新生成的30元先享月费账单，每笔比原来多15元
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 查询当前钱包 `paid_monthly_amount` 基线：
     ```sql
     SELECT w.WALLET_ID, w.paid_monthly_amount, t.OPERATOR_CODE
     FROM hcb_complete_wallet w
     JOIN hcb_hlj_truckuserwallet hw ON w.WALLET_ID = hw.WALLET_ID
     JOIN hcb_truckuser t ON hw.ID_CODE = t.ID_CODE
     WHERE t.OPERATOR_CODE IN ('XTK', 'TXB')
     LIMIT 10;
     ```
  2. 查询该钱包对应的历史先享月费账单笔数（已扣款成功，`bill_status=1`）：
     ```sql
     SELECT wallet_id, COUNT(*) AS bill_count
     FROM hcb_monthly_paylater_wallet_bill
     WHERE wallet_id = '你的wallet_id' AND bill_status = 1
     GROUP BY wallet_id;
     ```
  3. 执行全量更新SQL（每笔账单 +15 元）：
     ```sql
     UPDATE hcb_complete_wallet cw
     SET cw.paid_monthly_amount = cw.paid_monthly_amount + (
       SELECT COUNT(*) * 15
       FROM hcb_monthly_paylater_wallet_bill mb
       WHERE mb.wallet_id = cw.WALLET_ID
         AND mb.bill_status = 1
         AND mb.bill_date BETWEEN 20260101 AND 20260313
     )
     WHERE cw.WALLET_ID IN (
       SELECT DISTINCT wallet_id FROM hcb_monthly_paylater_wallet_bill
       WHERE bill_status = 1 AND bill_date BETWEEN 20260101 AND 20260313
     );
     ```
  4. 验证更新后 `paid_monthly_amount` 是否正确增加
- **预期结果**:
  - `paid_monthly_amount` = 更新前值 + 该钱包1-3月份扣款成功账单笔数 × 15
  - 无 XTK/TXB 钱包被遗漏
  - LTK/MTK 钱包不被更新

---

#### TC-019 paid_monthly_amount更新后数据一致性验证
- **前置条件**:
  - 数据库: hcb
  - 已完成 TC-018 的更新
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 抽查5个 XTK/TXB 钱包，分别验证：
     ```sql
     SELECT
       cw.WALLET_ID,
       cw.paid_monthly_amount AS 当前值,
       COUNT(mb.id) AS 扣款成功笔数,
       COUNT(mb.id) * 30 AS 理论应付金额
     FROM hcb_complete_wallet cw
     JOIN hcb_monthly_paylater_wallet_bill mb ON cw.WALLET_ID = mb.wallet_id
     WHERE mb.bill_status = 1
       AND mb.operator_code IN ('XTK', 'TXB')
     GROUP BY cw.WALLET_ID, cw.paid_monthly_amount
     HAVING COUNT(mb.id) > 0
     LIMIT 5;
     ```
  2. 人工核对理论应付金额与 `paid_monthly_amount` 是否一致
- **预期结果**: `paid_monthly_amount` 与扣款成功账单总金额（笔数×30）一致

---

## 8. 扣款异常场景验证

### 8.1 钱包冻结金额场景

#### TC-020 钱包冻结金额充足（>=30元）-扣款成功
- **前置条件**:
  - 数据库: hcb
  - 准备测试用户，`FREEZE_AMOUNT = 100`，`WITHHOLD_FREEZE_AMOUNT = 0`
  - 待扣款先享月费账单 `bill_amount = 30`，`bill_status = 0`
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 设置钱包余额：
     ```sql
     UPDATE hcb_hlj_truckuserwallet
     SET FREEZE_AMOUNT = 100.00, WITHHOLD_FREEZE_AMOUNT = 0.00
     WHERE ID_CODE = '你的身份证';
     ```
  2. 触发扣款：
     ```bash
     curl "http://127.0.0.1:8082/hcbTimer-business/test/startMonthlyTaskByIdCode.do?idCodeStrs=你的身份证"
     ```
  3. 查询账单状态和流水：
     ```sql
     SELECT bill_status, actual_amount, remark FROM hcb_monthly_paylater_wallet_bill
     WHERE id_code = '你的身份证' ORDER BY update_time DESC LIMIT 3;
     
     SELECT TRADE_TYPE, TRADE_AMOUNT, FUND_DIRECTION FROM hcb_hlj_etc_flow
     WHERE USER_ID = '你的车辆ID' AND TRADE_TYPE = 29 ORDER BY TRADE_TIME DESC LIMIT 3;
     ```
- **预期结果**:
  - `bill_status = 1`（扣款成功）
  - `actual_amount = 30.00`
  - `hcb_hlj_etc_flow` 新增 `TRADE_TYPE=29`，`TRADE_AMOUNT=30`，`FUND_DIRECTION=ADD`

---

#### TC-021 钱包冻结金额不足（>0但<30元）-实扣可用金额并标记失败
- **前置条件**:
  - 数据库: hcb
  - 测试用户 `FREEZE_AMOUNT = 20`，`WITHHOLD_FREEZE_AMOUNT = 0`
  - 待扣款先享月费账单 `bill_amount = 30`，`bill_status = 0`
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 设置钱包余额（可用=20，小于30）：
     ```sql
     UPDATE hcb_hlj_truckuserwallet
     SET FREEZE_AMOUNT = 20.00, WITHHOLD_FREEZE_AMOUNT = 0.00
     WHERE ID_CODE = '你的身份证';
     ```
  2. 触发扣款
  3. 查询账单状态和实扣金额
- **预期结果**:
  - `bill_status = 2`（扣款失败）
  - `actual_amount = 20.00`（实扣可用金额，非0也非30）
  - `remark` = 冻结金额不足相关提示

---

#### TC-022 钱包冻结金额=0-扣款失败
- **前置条件**:
  - 数据库: hcb
  - 测试用户 `FREEZE_AMOUNT = 0` 或 `FREEZE_AMOUNT <= WITHHOLD_FREEZE_AMOUNT`
  - 待扣款先享月费账单 `bill_status = 0`
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 设置钱包冻结金额为0：
     ```sql
     UPDATE hcb_hlj_truckuserwallet
     SET FREEZE_AMOUNT = 0.00, WITHHOLD_FREEZE_AMOUNT = 0.00
     WHERE ID_CODE = '你的身份证';
     ```
  2. 触发扣款
  3. 查询账单状态
- **预期结果**:
  - `bill_status = 2`（扣款失败）
  - `remark` = "冻结金额不足"
  - 无扣款流水生成

---

### 8.2 用户标签场景

#### TC-023 无Silent_Wallet标签-不允许扣款
- **前置条件**:
  - 数据库: hcb
  - 测试用户钱包无 `Silent_Wallet` 标签，也无 `MONTHLY_PAY_LATER_COMPLAINT_WALLET` 标签
  - 待扣款先享月费账单 `bill_status = 0`
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 确认用户无标签：
     ```sql
     SELECT * FROM hcb_label_config_manage_detail
     WHERE wallet_id = '你的钱包ID' AND label_key IN ('Silent_Wallet', 'MONTHLY_PAY_LATER_COMPLAINT_WALLET');
     ```
  2. 触发扣款
  3. 查询账单状态和备注
- **预期结果**:
  - `bill_status = 2`（扣款失败）
  - `remark` = "不允许扣款"（`MonthlyBillRemarkEnum.NOT_ALLOWED_DEDUCTIONS`）
  - 无扣款流水

---

#### TC-024 有MONTHLY_PAY_LATER_COMPLAINT_WALLET标签-客诉用户不扣款
- **前置条件**:
  - 数据库: hcb
  - 测试用户同时有 `Silent_Wallet` 和 `MONTHLY_PAY_LATER_COMPLAINT_WALLET` 标签
  - 待扣款先享月费账单 `bill_status = 0`
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 确认用户有客诉标签：
     ```sql
     SELECT label_key, label_status
     FROM hcb_label_config_manage_detail
     WHERE wallet_id = '你的钱包ID';
     ```
  2. 触发扣款
  3. 查询账单状态
- **预期结果**:
  - `bill_status = 2`（扣款失败）
  - `remark` = "客诉用户不允许扣款"（`MonthlyBillRemarkEnum.CUSTOMER_COMPLAINTS_NOT_ALLOWED_DEDUCTIONS`）

---

#### TC-025 钱包不存在-扣款失败处理
- **前置条件**:
  - 数据库: hcb
  - 构造一笔 wallet_id 不存在的先享月费账单
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 插入一笔 wallet_id 为不存在ID的先享月费账单（`bill_status=0`）
  2. 触发扣款
  3. 查询账单状态
- **预期结果**:
  - `bill_status = 2`（扣款失败）
  - `remark` = "钱包不存在"（`MonthlyBillRemarkEnum.WALLET_NOT_EXIST`）

---

### 8.3 风控任务配置异常

#### TC-026 Fee_MonthlyPayLater_Wallet任务未启用-账单生成终止
- **前置条件**:
  - 数据库: hcb
  - 将 `hcb_label_security_task` 中 `Fee_MonthlyPayLater_Wallet` 的 `task_status` 改为 `2`（禁用）
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 禁用任务：
     ```sql
     UPDATE hcb_label_security_task SET task_status = '2' WHERE task_code = 'Fee_MonthlyPayLater_Wallet';
     ```
  2. 手动触发账单生成
  3. 查看日志
  4. **恢复配置**：
     ```sql
     UPDATE hcb_label_security_task SET task_status = '1' WHERE task_code = 'Fee_MonthlyPayLater_Wallet';
     ```
- **预期结果**:
  - 任务提前终止，无新账单生成
  - 日志: `先享月费钱包账单生成任务未启用！！！`

---

#### TC-027 Fee_MonthlyPayLater_Wallet运营商列表为空-账单生成终止
- **前置条件**:
  - 数据库: hcb
  - 将 `task_require` 改为格式错误（如只有标签名，无运营商列表）
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 修改配置：
     ```sql
     UPDATE hcb_label_security_task SET task_require = 'Silent_Wallet' WHERE task_code = 'Fee_MonthlyPayLater_Wallet';
     ```
  2. 触发账单生成任务
  3. 查看日志
  4. **恢复配置**（还原为正确格式）
- **预期结果**:
  - 任务终止，日志: `先享月费钱包账单生成配置信息异常`或`运营商编码列表为空`

---

## 9. 退款场景验证

### 9.1 登录退款

#### TC-028 登录小程序触发先享月费自动退款（XTK/TXB）
- **前置条件**:
  - 数据库: hcb
  - 测试用户有已扣款先享月费账单（`bill_status=1`）
  - `hcb_operator` 中 XTK/TXB 的 `monthly_paylater_refund_flag = 1`
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 确认退款开关：
     ```sql
     SELECT CODE, monthly_paylater_refund_flag FROM hcb_operator WHERE CODE IN ('XTK', 'TXB');
     ```
  2. 确认待退款账单：
     ```sql
     SELECT id, wallet_id, bill_amount, actual_amount, bill_status
     FROM hcb_monthly_paylater_wallet_bill
     WHERE id_code = '你的身份证' AND bill_status = 1;
     ```
  3. 触发登录退款接口：
     ```bash
     curl -X POST "http://192.168.1.60:8092/hcbapi/monthlyPayLater/autoRefund" \
       -H "token: xxx" \
       -d "walletId=你的钱包ID"
     ```
  4. 查询退款结果：
     ```sql
     SELECT bill_status, refund_amount, refund_time
     FROM hcb_monthly_paylater_wallet_bill
     WHERE id_code = '你的身份证' ORDER BY update_time DESC;
     
     SELECT TRADE_TYPE, TRADE_AMOUNT, FUND_DIRECTION FROM hcb_hlj_etc_flow
     WHERE WALLET_ID = '你的钱包ID' AND TRADE_TYPE = 32 ORDER BY TRADE_TIME DESC LIMIT 3;
     ```
- **预期结果**:
  - `bill_status = 3`（已退款）
  - `refund_amount = actual_amount`
  - `hcb_hlj_etc_flow` 新增 `TRADE_TYPE=32`，`FUND_DIRECTION=SUB`

---

#### TC-029 退款开关关闭-登录不触发退款
- **前置条件**:
  - 数据库: hcb
  - `hcb_operator` 中 XTK 的 `monthly_paylater_refund_flag = 0`
  - 测试用户有已扣款先享月费账单
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 关闭退款开关：
     ```sql
     UPDATE hcb_operator SET monthly_paylater_refund_flag = 0 WHERE CODE = 'XTK';
     ```
  2. 触发登录退款接口
  3. 查询账单状态是否改变
  4. **恢复配置**：
     ```sql
     UPDATE hcb_operator SET monthly_paylater_refund_flag = 1 WHERE CODE = 'XTK';
     ```
- **预期结果**: 账单 `bill_status` 不变（仍为1），无退款流水生成；日志记录"先享月费未开启自动退款"

---

#### TC-030 通行账单计费后触发先享月费通行退款（MQ消息）
- **前置条件**:
  - 数据库: hcb
  - 测试用户有已扣款先享月费账单（`bill_status=1`）
  - `monthly_paylater_refund_flag = 1`
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 造通行账单，执行账单计费任务：
     ```bash
     curl -X GET "http://192.168.1.60:8087/hcbTimer-bill/test/deductBillCalculatTask"
     ```
  2. 查看 hcbTimer-bill 日志，确认 MQ 消息发送：
     ```bash
     tail -n 300 /home/tomcat/tomcat-hcbTimer-bill/logs/catalina.out | grep "先享月费账单通行退款监听\|monthlyPayLaterWalletBillRefundQueue"
     ```
  3. 等待 MQ 消费完成，查询退款结果：
     ```sql
     SELECT bill_status, refund_amount, refund_time FROM hcb_monthly_paylater_wallet_bill
     WHERE id_code = '你的身份证' ORDER BY update_time DESC;
     ```
  4. 确认退款流水：
     ```sql
     SELECT TRADE_TYPE, ORDER_NO FROM hcb_hlj_etc_flow
     WHERE WALLET_ID = '你的钱包ID' AND TRADE_TYPE = 31 ORDER BY TRADE_TIME DESC LIMIT 3;
     ```
- **预期结果**:
  - `bill_status = 3`（已退款）
  - `hcb_hlj_etc_flow` 新增 `TRADE_TYPE=31`，`ORDER_NO` = 通行账单ID
  - 日志: `先享月费退款成功`

---

## 10. 回归验证

### 10.1 原有功能不受影响

#### TC-031 LTK/MTK运营商先享月费流程不受本次改动影响
- **前置条件**:
  - 数据库: hcb
  - LTK/MTK 用户有月管理费失败账单
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 查询 LTK/MTK 的失败账单，确认金额不为30（按原配置）
  2. 手动触发账单生成（date=今天）
  3. 查询 LTK/MTK 生成的先享月费账单金额
- **预期结果**: LTK/MTK 先享月费账单金额与其月管理费失败账单金额一致（非强制30元）

---

#### TC-032 hcb_complete_wallet_bill中成功账单不受历史初始化影响
- **前置条件**:
  - 数据库: hcb
  - 存在 `bill_status = 1`（成功）的 XTK/TXB 月管理费账单
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 记录成功账单金额基线
  2. 执行初始化 SQL（WHERE条件中含 `bill_status = 2`）
  3. 再次查询成功账单金额
- **预期结果**: `bill_status = 1`（成功）的账单金额不变，初始化操作仅更新失败账单

---

#### TC-033 已生成的先享月费账单不被重复生成
- **前置条件**:
  - 数据库: hcb
  - 某用户已有 `bill_status=0/1` 的先享月费账单
  - 对应的月管理费失败账单仍在时间窗口内
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 记录已有先享月费账单数量
  2. 再次手动触发账单生成（同一 date 参数）
  3. 查询先享月费账单数量是否增加
- **预期结果**: 不重复生成，账单数量不变（`generateMonthlyPaylaterWalletBill` 内部有幂等性保护）

---

#### TC-034 其他定时任务（非先享月费）正常运行
- **前置条件**:
  - 系统: 任货行定时任务 (hcbTimer-business)
  - 测试环境: 192.168.1.60
- **测试步骤**:
  1. 触发其他常规定时任务（如账单解析、无通行用户扣款等）
  2. 查看日志确认正常执行
- **预期结果**: 其他定时任务不受本次改动影响，正常执行

---

## 11. 补充业务场景验证（根据最新脑图）

### 11.1 先享后付失效逻辑

#### TC-035 先享后付-扣款前发生通行导致账单失效
- **前置条件**:
  - 数据库: hcb
  - 用户: 湖南XTK或江苏TXB用户，已有待扣款先享月费账单（`bill_status=0`）
  - 测试环境: 192.168.1.60
- **测试步骤**:
  - 1. 确认用户已有待扣款账单：
    ```sql
    SELECT id, bill_status FROM hcb_monthly_paylater_wallet_bill 
    WHERE id_code = '你的身份证' AND bill_status = 0;
    ```
  - 2. 模拟发生通行：在 `hcb_complete_wallet_bill` 中插入一笔该用户的通行账单（非月管理费），或直接触发通行计费任务。
  - 3. 触发扣款任务或等待系统处理。
  - 4. 查询先享月费账单状态。
- **预期结果**: 
  - 待扣款账单状态变为“失效”或被标记为不扣款（具体表现需根据代码实现确认，若代码未实现则记录为BUG）。
  - 扣款任务不再对该笔账单执行扣款。

---

### 11.2 按月扣（静默钱包）扣款逻辑

#### TC-036 按月扣-扣款优先级验证（余额 -> 余额扣完 -> 冻结金额）
- **前置条件**:
  - 数据库: hcb
  - 用户: 黑龙江LTK或广西MTK用户，有 `Silent_Wallet` 标签
  - 钱包状态: `BALANCE = 10.00`, `FREEZE_AMOUNT = 50.00`, `WITHHOLD_FREEZE_AMOUNT = 0`
  - 扣款金额: 15.00
- **测试步骤**:
  - 1. 设置钱包余额和冻结金额：
    ```sql
    UPDATE hcb_hlj_truckuserwallet 
    SET BALANCE = 10.00, FREEZE_AMOUNT = 50.00, WITHHOLD_FREEZE_AMOUNT = 0.00
    WHERE ID_CODE = '你的身份证';
    ```
  - 2. 触发 `eTCBuckleFee` 任务：
    ```bash
    curl "http://192.168.1.60:8087/hcbTimer-bill/test/testETCBuckleFee.do"
    ```
  - 3. 查询钱包余额和流水。
- **预期结果**: 
  - 优先扣除余额 10.00（余额变为 0）。
  - 剩余 5.00 从冻结金额中扣除（冻结金额变为 45.00）。
  - 生成两笔流水或一笔组合扣款流水。

#### TC-037 按月扣-冻结金额不足15元扣完即止
- **前置条件**:
  - 数据库: hcb
  - 用户: 有 `Silent_Wallet` 标签
  - 钱包状态: `BALANCE = 0`, `FREEZE_AMOUNT = 8.00`, `WITHHOLD_FREEZE_AMOUNT = 0`
  - 扣款金额: 15.00
- **测试步骤**:
  - 1. 设置钱包状态：
    ```sql
    UPDATE hcb_hlj_truckuserwallet 
    SET BALANCE = 0.00, FREEZE_AMOUNT = 8.00, WITHHOLD_FREEZE_AMOUNT = 0.00
    WHERE ID_CODE = '你的身份证';
    ```
  - 2. 触发 `eTCBuckleFee` 任务。
  - 3. 查询钱包状态和生成的账单。
- **预期结果**: 
  - 实扣金额为 8.00（冻结金额扣至 0）。
  - 账单标记为成功（或按实际扣款金额记录）。
  - 下个月不再继续扣除该笔欠款（扣完即止）。

---

## 附录：测试数据快速造数参考

### A. 查询测试账号信息
```sql
-- 查询XTK/TXB用户的钱包和车辆信息
SELECT 
    t.TRUCKUSER_ID, t.CAR_NUM, t.OPERATOR_CODE, t.ID_CODE,
    w.WALLET_ID, w.FREEZE_AMOUNT, w.WITHHOLD_FREEZE_AMOUNT
FROM hcb_truckuser t
JOIN hcb_hlj_truckuserwallet w ON t.ID_CODE = w.ID_CODE
WHERE t.OPERATOR_CODE IN ('XTK', 'TXB')
  AND t.STATUS = 1
LIMIT 10;
```

### B. 造月管理费失败账单（30元）
```sql
INSERT INTO hcb_complete_wallet_bill
(id, wallet_id, user_id, id_code, user_name, car_num, bill_date,
 operator_code, bill_amount, fail_amount, bill_type, bill_status,
 create_by, update_by, create_time, update_time)
VALUES
(90001, '你的钱包ID', '你的车辆ID', '你的身份证', '测试用户', '测试车牌',
 20260310, 'XTK', 30.00, 30.00, 1, 2, 'test', 'test', NOW(), NOW());
```

### C. 核心接口汇总

| 接口 | 命令 |
|------|------|
| 账单生成（指定日期） | `curl "http://127.0.0.1:8082/hcbTimer-business/test/startMonthlyTask.do?type=1&date=20260302"` |
| 账单扣款（全量） | `curl "http://127.0.0.1:8082/hcbTimer-business/test/startMonthlyTask.do?type=2&date=20260302"` |
| 账单扣款（按身份证） | `curl "http://127.0.0.1:8082/hcbTimer-business/test/startMonthlyTaskByIdCode.do?idCodeStrs=身份证"` |
| 登录退款 | `curl -X POST "http://192.168.1.60:8092/hcbapi/monthlyPayLater/autoRefund" -d "walletId=xxx"` |
| 通行计费任务 | `curl "http://192.168.1.60:8087/hcbTimer-bill/test/deductBillCalculatTask"` |
| 无通行钱包用户扣款任务 | `curl "http://192.168.1.60:8087/hcbTimer-bill/test/testETCBuckleFee.do"` |

### D. 日志路径

| 系统 | 日志路径 |
|------|---------|
| hcbTimer-business | `/home/tomcat/tomcat-hcbTimer-business/logs/catalina.out` |
| hcbTimer-bill | `/home/tomcat/tomcat-hcbTimer-bill/logs/catalina.out` |
| hcbapi | `/home/tomcat/tomcat-hcbapi/logs/catalina.out` |

### E. 关键数据表状态枚举

| 表 | 字段 | 枚举值 |
|----|------|--------|
| `hcb_complete_wallet_bill` | `bill_type` | 1=月管理费 |
| `hcb_complete_wallet_bill` | `bill_status` | 1=成功，2=失败，3=已退款 |
| `hcb_monthly_paylater_wallet_bill` | `bill_status` | 0=待扣款，1=成功，2=失败，3=已退款 |
| `hcb_hlj_etc_flow` | `TRADE_TYPE` | 29=先享月费扣款，30=人工退款，31=通行退款，32=登录退款 |
| `hcb_hlj_etc_flow` | `FUND_DIRECTION` | ADD=增加（扣款），SUB=减少（退款） |
