# 任货行湖南划账扣款文件回盘优化测试用例（完善版）

## 1. 数据源切换功能测试

### 1.1 数据源查询逻辑测试

#### TC-001 客车落库数据查询测试
- **前置条件**：客车系统有落库的账单数据（rtx.rtx_etc_deducrecord_xtk表中有XTK数据）
- **测试步骤**：
  1. 在rtx_etc_deducrecord_xtk表中插入测试数据（operator_code='XTK'）
  2. 触发湖南划账扣款文件回盘处理：
     - 调用接口：`GET http://192.168.1.60:8087/hcbTimer-bill/test/HnTransferResult`
     - 调用接口：`GET http://192.168.1.60:8087/hcbTimer-bill/test/HnUploadRefundResult`
  3. 验证系统是否从客车落库数据中查询（rtx_etc_deducrecord_xtk表）
  4. 确认不再从mongo记录中查询（Constants.MONGODB_TROLLEY_BILL集合）
- **预期结果**：系统正确从客车落库数据查询，不依赖mongo记录
- **技术验证点**：
  - 验证调用 `heideducrecordService.findBy(pageData)` 方法
  - 确认不调用 `mongoTemplate.find(query, PageData.class, Constants.MONGODB_TROLLEY_BILL)`
  - 验证从rtx_etc_deducrecord_xtk表查询operator_code='XTK'的数据
- **验证SQL**：
  ```sql
  -- 验证测试数据被查询和处理
  SELECT trading_number, status, callback_flag, update_time
  FROM rtx.rtx_etc_deducrecord_xtk 
  WHERE update_by = 'TC001_TEST'
    AND update_time > DATE_SUB(NOW(), INTERVAL 30 MINUTE)
  ORDER BY update_time DESC;
  ```

#### TC-002 数据源切换前后对比测试
- **前置条件**：同时存在mongo记录和客车落库数据
- **测试步骤**：
  1. 确认mongo中存在MONGODB_TROLLEY_BILL集合的账单记录
  2. 在rtx_etc_deducrecord_xtk表中存在相同账单的落库记录
  3. 触发回盘处理（调用上述两个接口）
  4. 验证系统优先使用客车落库数据
  5. 通过日志确认不会使用mongo中的记录
- **预期结果**：系统优先使用客车落库数据，忽略mongo记录
- **技术验证点**：
  - 验证 `heideducrecordService.findBy()` 返回的数据被使用
  - 确认 `Constants.MONGODB_TROLLEY_BILL` 集合未被查询
  - 监控数据库查询日志验证查询来源

#### TC-003 客车落库数据缺失处理测试
- **前置条件**：mongo中有记录但客车系统未落库，或落库数据为空
- **测试步骤**：
  1. 设置场景：mongo中存在账单记录，但rtx_etc_deducrecord_xtk表中该账单未落库
  2. 触发回盘处理
  3. 验证系统的处理逻辑
  4. 检查是否有异常处理机制
  5. 确认是否记录"id未找到数据......."错误日志
- **预期结果**：系统能正确处理客车落库数据缺失的情况，有合理的异常处理
- **技术验证点**：
  - 验证 `CollectionUtils.isEmpty(findBy)` 判断逻辑
  - 确认记录 "id未找到数据......." 错误日志
  - 验证执行 `continue` 跳过当前记录
- **验证方法**：
  ```bash
  # 检查应用日志中的错误记录
  grep -i "id未找到数据" /path/to/application.log
  ```

### 1.2 数据一致性验证测试

#### TC-004 客车落库数据完整性测试
- **前置条件**：rtx_etc_deducrecord_xtk表有多笔XTK账单落库
- **测试步骤**：
  1. 验证客车落库数据的字段完整性
  2. 检查关键字段（trading_number、fee、bill_date、operator_code等）是否齐全
  3. 验证数据格式的正确性（bill_date格式为YYYY-MM-DD）
  4. 确认数据与原始账单的一致性
- **预期结果**：客车落库数据完整准确，格式正确
- **技术验证点**：
  - 验证 `pageData.put("BILL_DATE", date)` 数据设置
  - 确认 `OPERATOR_CODE` 字段正确设置为'XTK'
  - 验证 `trading_number` 字段的唯一性和格式
- **验证SQL**：
  ```sql
  -- 检查数据完整性
  SELECT 
      COUNT(*) as total_records,
      COUNT(trading_number) as has_trading_number,
      COUNT(fee) as has_fee,
      COUNT(CASE WHEN bill_date REGEXP '^[0-9]{4}-[0-9]{2}-[0-9]{2}$' THEN 1 END) as valid_date_format,
      COUNT(CASE WHEN operator_code = 'XTK' THEN 1 END) as xtk_records
  FROM rtx.rtx_etc_deducrecord_xtk 
  WHERE bill_date = DATE_FORMAT(CURDATE(), '%Y-%m-%d');
  ```

#### TC-005 getRtxHnBill方法查询测试
- **前置条件**：rtx_etc_deducrecord_xtk表中有XTK数据
- **测试步骤**：
  1. 准备测试数据，确保有特定日期的XTK账单
  2. 触发回盘任务
  3. 验证getRtxHnBill方法的查询逻辑
  4. 检查返回的数据格式是否符合要求
- **预期结果**：getRtxHnBill方法能正确查询并返回格式化的数据
- **技术验证点**：
  - 验证查询SQL：`SELECT fee FEE,trading_number ID,media_type MEDIA_TYPE,create_time CREATE_TIME,ex_time EX_TIME, bill_date BILL_DATE FROM rtx.rtx_etc_deducrecord_xtk WHERE bill_date = #{BILL_DATE}`
  - 确认返回数据的字段映射：fee→FEE, trading_number→ID等
  - 验证 `XTKList.get(0).getString("BILL_DATE")` 数据提取正确
- **模拟验证SQL**：
  ```sql
  -- 模拟getRtxHnBill查询
  SELECT 
      fee as FEE,
      trading_number as ID,
      media_type as MEDIA_TYPE,
      create_time as CREATE_TIME,
      ex_time as EX_TIME,
      bill_date as BILL_DATE
  FROM rtx.rtx_etc_deducrecord_xtk
  WHERE bill_date = '2024-12-17'  -- 替换为测试日期
    AND operator_code = 'XTK'
  ORDER BY create_time DESC;
  ```

## 2. 具体接口测试

### 2.1 划账结果生成接口测试

#### TC-006 HnTransferResult接口调用测试
- **前置条件**：系统正常运行，有XTK测试数据
- **测试步骤**：
  1. 准备测试数据（昨天的通行文件数据）
  2. 调用接口：`GET http://192.168.1.60:8087/hcbTimer-bill/test/HnTransferResult`
  3. 验证接口返回状态码和响应内容
  4. 检查是否触发了划账结果生成逻辑
- **预期结果**：接口调用成功，返回200状态码，触发相应的业务逻辑
- **技术验证点**：
  - 验证 `uploadDeductionResult()` 方法被调用
  - 确认处理"昨天的通行文件"逻辑
  - 验证通行文件的生成和处理
- **接口测试命令**：
  ```bash
  curl -v "http://192.168.1.60:8087/hcbTimer-bill/test/HnTransferResult"
  ```

#### TC-007 HnUploadRefundResult接口调用测试
- **前置条件**：系统正常运行，有退费交易数据
- **测试步骤**：
  1. 准备退费交易测试数据
  2. 调用接口：`GET http://192.168.1.60:8087/hcbTimer-bill/test/HnUploadRefundResult`
  3. 验证接口返回状态码和响应内容
  4. 检查是否正确处理zip文件和json数据
- **预期结果**：接口调用成功，正确处理退费交易的zip文件
- **技术验证点**：
  - 验证 `uploadRefundResult()` 方法被调用
  - 确认"1个zip文件中1个json对应一笔退费交易"的处理逻辑
  - 验证zip文件的生成和上传
- **接口测试命令**：
  ```bash
  curl -v "http://192.168.1.60:8087/hcbTimer-bill/test/HnUploadRefundResult"
  ```

### 2.2 接口异常处理测试

#### TC-008 接口超时处理测试
- **前置条件**：网络环境允许模拟超时
- **测试步骤**：
  1. 模拟网络延迟或超时情况
  2. 调用两个接口
  3. 验证超时处理机制
  4. 检查错误日志和重试逻辑
- **预期结果**：系统能正确处理接口超时，有合理的重试和降级机制

#### TC-009 接口异常响应处理测试
- **前置条件**：服务可能返回异常状态码
- **测试步骤**：
  1. 模拟服务返回非200状态码（如500、404等）
  2. 验证系统的异常处理逻辑
  3. 检查错误日志记录
  4. 验证是否有告警机制
- **预期结果**：系统能正确处理异常响应，记录详细的错误信息

## 3. 客车重复推送账单处理测试

### 3.1 重复账单识别测试

#### TC-010 客车重复推送账单识别测试
- **前置条件**：rtx_etc_deducrecord_xtk表中有重复的trading_number数据
- **测试步骤**：
  1. 插入相同trading_number但不同id的测试数据
  2. 触发回盘处理
  3. 验证系统是否能识别重复推送
  4. 检查重复账单的处理逻辑
- **预期结果**：系统能正确识别客车重复推送的账单
- **技术验证点**：
  - 验证系统能识别相同的 `trading_number`
  - 确认 `XTKList.get(0).getString("BILL_DATE")` 数据获取的唯一性处理
- **准备测试数据SQL**：
  ```sql
  -- 插入重复trading_number的测试数据
  INSERT INTO rtx.rtx_etc_deducrecord_xtk (
      id, trading_number, bill_date, operator_code, fee, status, 
      create_by, update_by, create_time, update_time
  ) VALUES 
  (CONCAT(UNIX_TIMESTAMP(), '101'), 'DUPLICATE_TEST_001', DATE_FORMAT(CURDATE(), '%Y-%m-%d'), 'XTK', '5.00', '0', 'TEST', 'TC010_TEST', NOW(), NOW()),
  (CONCAT(UNIX_TIMESTAMP(), '102'), 'DUPLICATE_TEST_001', DATE_FORMAT(CURDATE(), '%Y-%m-%d'), 'XTK', '5.00', '0', 'TEST', 'TC010_TEST', NOW(), NOW());
  ```

#### TC-011 重复数据去重处理测试
- **前置条件**：存在重复推送的客车账单
- **测试步骤**：
  1. 确认存在重复trading_number的数据
  2. 触发回盘处理
  3. 验证系统的去重处理逻辑
  4. 检查最终处理结果的唯一性
- **预期结果**：系统正确处理重复数据，确保业务逻辑的正确性
- **验证SQL**：
  ```sql
  -- 检查重复数据处理结果
  SELECT 
      trading_number,
      COUNT(*) as count,
      GROUP_CONCAT(DISTINCT status) as statuses,
      MAX(update_time) as last_update
  FROM rtx.rtx_etc_deducrecord_xtk 
  WHERE update_by = 'TC010_TEST'
  GROUP BY trading_number
  HAVING COUNT(*) > 1;
  ```

## 4. 实际收款方统计逻辑测试

### 4.1 actual_payee字段处理测试

#### TC-012 分米收款记录处理测试
- **前置条件**：rtx_etc_deducrecord_xtk表中有actual_payee=1的记录
- **测试步骤**：
  1. 插入actual_payee=1（分米收款）的测试数据
  2. 触发回盘处理
  3. 验证分米收款的统计逻辑
  4. 检查统计结果的准确性
- **预期结果**：系统正确识别和统计分米收款的记录
- **验证SQL**：
  ```sql
  -- 验证分米收款统计
  SELECT 
      actual_payee,
      CASE WHEN actual_payee = 1 THEN '分米收款' ELSE '其他' END as payee_desc,
      COUNT(*) as count,
      SUM(CAST(fee AS DECIMAL(10,2))) as total_amount
  FROM rtx.rtx_etc_deducrecord_xtk 
  WHERE operator_code = 'XTK'
    AND actual_payee = 1
    AND bill_date = DATE_FORMAT(CURDATE(), '%Y-%m-%d');
  ```

#### TC-013 运营商收款记录处理测试
- **前置条件**：rtx_etc_deducrecord_xtk表中有actual_payee=2的记录
- **测试步骤**：
  1. 插入actual_payee=2（运营商收款）的测试数据
  2. 触发回盘处理
  3. 验证运营商收款的统计逻辑
  4. 检查统计结果是否按运营商正确归类
- **预期结果**：系统正确识别和统计运营商收款的记录
- **验证SQL**：
  ```sql
  -- 验证运营商收款统计
  SELECT 
      actual_payee,
      CASE WHEN actual_payee = 2 THEN '运营商收款' ELSE '其他' END as payee_desc,
      COUNT(*) as count,
      SUM(CAST(fee AS DECIMAL(10,2))) as total_amount
  FROM rtx.rtx_etc_deducrecord_xtk 
  WHERE operator_code = 'XTK'
    AND actual_payee = 2
    AND bill_date = DATE_FORMAT(CURDATE(), '%Y-%m-%d');
  ```

## 5. 综合业务流程测试

### 5.1 端到端完整流程测试

#### TC-014 湖南XTK完整回盘流程测试
- **前置条件**：有完整的XTK测试数据，包括不同状态和收款方的记录
- **测试步骤**：
  1. 准备多样化的测试数据（正常、重复、不同收款方）
  2. 依次调用两个接口：
     - `GET http://192.168.1.60:8087/hcbTimer-bill/test/HnTransferResult`
     - `GET http://192.168.1.60:8087/hcbTimer-bill/test/HnUploadRefundResult`
  3. 验证完整的业务流程执行
  4. 检查数据处理的完整性和准确性
- **预期结果**：完整的回盘流程顺利执行，各环节数据准确处理
- **技术验证点**：
  - 确认 `heideducrecordService.findBy()` 方法正确调用
  - 验证 `getRtxHnBill()` 方法返回正确数据
  - 确认数据源完全切换到客车落库（rtx_etc_deducrecord_xtk表）

#### TC-015 混合场景处理测试
- **前置条件**：同时存在正常数据、重复数据、不同收款方数据
- **测试步骤**：
  1. 准备包含各种情况的测试数据集
  2. 触发回盘处理
  3. 验证系统对不同类型数据的处理能力
  4. 检查处理结果的一致性和准确性
- **预期结果**：系统能正确处理各种混合场景，结果准确可靠

## 6. 性能和监控测试

### 6.1 性能测试

#### TC-016 大数据量处理性能测试
- **前置条件**：rtx_etc_deducrecord_xtk表中有大量XTK数据
- **测试步骤**：
  1. 准备1000+条XTK测试数据
  2. 触发回盘处理
  3. 监控处理时间和资源消耗
  4. 对比优化前后的性能差异
- **预期结果**：处理性能良好，优化后性能不劣于优化前
- **性能指标**：
  - 查询响应时间 < 5秒
  - 内存使用合理
  - CPU占用率 < 80%

#### TC-017 并发处理测试
- **前置条件**：支持并发调用的环境
- **测试步骤**：
  1. 同时调用多个回盘接口
  2. 验证系统的并发处理能力
  3. 检查数据一致性
  4. 验证无资源竞争问题
- **预期结果**：系统能稳定处理并发请求，数据保持一致性

## 7. 回归测试

### 7.1 现有功能回归测试

#### TC-018 非XTK数据处理回归测试
- **前置条件**：rtx_etc_deducrecord_xtk表中有非XTK的其他运营商数据
- **测试步骤**：
  1. 确认表中有其他operator_code的数据
  2. 触发回盘处理
  3. 验证其他运营商数据不受XTK优化影响
  4. 检查处理逻辑的正确性
- **预期结果**：其他运营商的数据处理不受湖南XTK优化影响

#### TC-019 MongoDB查询功能回归测试
- **前置条件**：系统中仍有需要使用MongoDB的其他功能
- **测试步骤**：
  1. 验证其他业务的MongoDB查询功能
  2. 确认只有湖南XTK相关的查询切换到客车落库
  3. 检查MongoDB连接和查询的正常性
- **预期结果**：其他MongoDB查询功能正常，未受影响

## 8. 验收测试

### 8.1 需求实现验收

#### TC-020 需求实现完整性验证测试
- **前置条件**：了解完整的优化需求
- **测试步骤**：
  1. 验证数据源从mongo切换到客车落库（rtx_etc_deducrecord_xtk）的实现
  2. 验证重复推送账单处理的优化
  3. 验证回盘成功率的提升
  4. 确认所有接口和功能正常工作
- **预期结果**：所有优化需求都已完整实现，功能符合设计要求
- **技术验证点**：
  - 确认 `heideducrecordService.findBy()` 方法正确实现
  - 验证两个HTTP接口正常工作
  - 确认MongoDB查询逻辑已切换

#### TC-021 业务效果验证测试
- **前置条件**：湖南回盘优化功能已完整部署
- **测试步骤**：
  1. 对比优化前后的回盘成功率
  2. 验证重复流水号问题的解决
  3. 测试客车账单回盘的稳定性
  4. 评估整体优化效果
- **预期结果**：优化效果明显，回盘成功率显著提升，重复流水号问题得到解决

---

## 测试用例总结

本测试用例文档共包含 **21个测试案例**，基于实际的技术实现进行了全面完善：

### **主要完善内容：**

1. **具体化接口信息**：加入了实际的HTTP接口地址和调用方法
2. **精确的数据库操作**：基于实际表结构（rtx_etc_deducrecord_xtk）编写验证SQL
3. **详细的技术验证点**：明确了需要验证的具体方法和逻辑
4. **可执行的测试脚本**：提供了具体的SQL和命令行操作
5. **实际的字段映射**：基于真实的代码逻辑（如fee→FEE，trading_number→ID）
6. **具体的错误处理**：包含了实际的错误日志内容验证

### **测试覆盖范围：**
- **数据源切换功能测试**：5个测试案例
- **具体接口测试**：4个测试案例  
- **重复账单处理测试**：2个测试案例
- **收款方统计测试**：2个测试案例
- **综合业务流程测试**：2个测试案例
- **性能监控测试**：2个测试案例
- **回归测试**：2个测试案例
- **验收测试**：2个测试案例

现在的测试用例更加实用和可执行，能够准确验证湖南回盘优化的各个方面！ 